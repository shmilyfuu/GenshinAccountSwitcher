<Window
    x:Class="GenshinSwitcherXamlPrototype.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#F6F7F9">
        <Border Width="520" Padding="28" CornerRadius="16" Background="White" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Spacing="14">
                <TextBlock Text="WinUI 3 启动测试" FontSize="26" FontWeight="SemiBold" />
                <TextBlock Text="C++ · Windows App SDK 1.7 · 未打包 · 框架依赖" FontSize="13" Opacity="0.62" />
                <TextBlock Text="如果你能看到这个窗口，说明 WinUI 3 的标准 XAML 启动链路可以在这台电脑上工作。" TextWrapping="Wrap" FontSize="14" Margin="0,8,0,4" />
                <Button x:Name="TestButton" Content="点击测试" Width="120" HorizontalAlignment="Left" Click="OnTestClick" />
                <TextBlock x:Name="StatusText" Text="等待点击" FontSize="13" Opacity="0.70" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
